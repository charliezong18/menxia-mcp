[English](MILESTONES.md) · **中文**

# 里程碑

落地顺序在 [SPEC §8](SPEC.zh-CN.md)。这份补的是 spec 没有的那一半：**凭什么算做完。**

**「写完」不等于「做完」。** Phase 0 写完花了约一小时，做完是三小时后——中间隔着三次误伤，全靠测试才暴露，其中一次是**静默的安全洞**。所以下面每条判据都是可证伪的、且必须真观察到，不接受「代码写了」。

## 各阶段

| 阶段 | 完工判据（可证伪） | 状态 |
|---|---|---|
| **0 · 路由守卫** | `guard-pr-create.test.sh` 21/21；两条路径实机验过（该拦的真被拒、该放的真通过）；SKILL.md 与 SPEC §7 与代码三方一致 | **✅ 2026-07-28** |
| **1 · 只读工具** ✅ | `read_comments` 在真折上返回真批注，`answered` 与手数对得上；`list_folders` 的未回数一致 | **✅ 2026-07-29** |
| **2 · lint_folder** | 对全部 open 折 + 每条规则至少一个必失败样本跑 differential，新旧输出逐条对齐，差异要么修掉要么写进 SPEC §5.2 说清。**上线闸门实际换成了疤痕清单**（design D7），differential 降级为一次性对照 —— 理由：它会随老脚本一起死、真实语料 88% 空转、覆盖不到新增的两条规则 | **✅ 2026-07-30**（17/17 折对齐，6 处刻意改进逐条登记；疤痕清单变异杀伤 53/59） |
| **3 · 写入工具** | 用 `open_folder` 端到端真呈一折；两个并发 session **实测**不互踩（要看到锁生效，不是假设）；标记从线上 PR body 回读确认。**判据里的「flock」在这台机器上不存在**——Node v25 的 `fs.constants` 没有 `O_EXLOCK`，macOS 没有 `flock(1)`，改成 `O_EXCL` 锁文件 + 判活，判据的**实质**（看到串行化、崩溃后不死锁）原样保留 | **✅ 2026-07-30**（真呈 #38；两个真进程持锁窗口重叠 −30ms；标记线上回读 = 本会话 id） |
| **4 · 退休** | 四个 bash 脚本删掉后走完整整一轮：呈折 → 读批注 → 回话；SKILL.md 瘦成名词表 + 工具指路；hook 拒绝信息改指 MCP 工具；SPEC §5.3 #6 那笔文档账还清 | **✅ 2026-07-30**（真 stdio 传输跑完整轮，#42；SKILL.md 15.7KB→8.9KB 触发词一个没丢；三个 guard 改指后自验 21+26+24+18 全过；体例表整张删掉，那笔账随之了结） |

**最大的风险现在在 Phase 2，不在 Phase 1。** 原本 Phase 1 扛的是「agy 能不能挂上 MCP」——那条 2026-07-29 随 agy 出局而作废（[SPEC §1](SPEC.zh-CN.md)），不是被验证掉的，是被决策绕过的。**风险没有消失，只是换了位置**：现在最可能毁掉这个项目的是 Phase 2 的 lint 移植悄悄丢掉既有修复。那不是个能靠观察发现的失败——脚本删了、测试绿了、错误也照样发生，只有 differential test 拦得住。

## 实际发生了什么

按阶段追加，永不改写旧条目。

| 日期 | 阶段 | 发生了什么 |
|---|---|---|
| 2026-07-30 | 4 | **「走完整整一轮」这条判据当场证明了自己值这个价：它抓到一个 509 条单测全没抓到的 bug。** `reply_comment` 的 `commentId` 复用了折号那套校验（上界 `1_000_000`），而 GitHub 的批注 id 是 **10 位数**（实测 `3686996586`）—— 也就是说**这个工具对任何真实批注 id 都会拒**，报的还是「pr 得是正整数」这种指错字段的话。单测全绿是因为它们清一色用 `commentId: 2` 这种小数字。**只有真跑一轮才撞得到。** 校验拆成 `parseId(raw, field, max)`，折号仍卡 1e6（那道闸防的是科学计数法：`String(1e21) === '1e+21'`，GitHub 按 to_i 解析成 1），批注 id 放宽到 `MAX_SAFE_INTEGER`。**这一轮走的是真 MCP stdio 传输**（手写 JSON-RPC 行协议 spawn `dist/index.js`），不是 `handleTool` —— 后者绕过注册与传输层，是 Phase 3 自己留下的验收口子，这次补上了。**删之前先存档**：`~/.claude` 不在任何版本控制里，那四个脚本删掉就是永久没了，而 SPEC §5.1「照实现移植」一路引用它们的行号 —— 原样搬进 `retired/`，配双语 README 写清每个被谁取代、以及 `folder-lint.sh:58` 那个假通过 bug；`lint-differential.mjs` 改指存档，仍跑得动、仍对齐。**收尾时自己也栽了一次「命令全成功而结果是零」**：清理脚本里 `git worktree add $WT main` 因为 main 已被主工作树占用而失败，`set -e` 在那个上下文没拦住，脚本照样打印「推了」—— 而什么都没推。改成 `--detach` + `push HEAD:main` 才真的推上去。这个项目一整天都在抓这个形状的病，最后一步还是撞上了。 |
| 2026-07-30 | 3 | **设计里写的锁在这台机器上不存在，而照着写会得到一把静默失效的锁。** SPEC §4.2 与任务书都写 `flock` / `O_EXLOCK | O_NONBLOCK`；实测 Node v25.2.1 的 `fs.constants` 里根本没有 `O_EXLOCK`，macOS 也没有 `flock(1)`——`O_CREAT | O_RDWR | undefined | O_NONBLOCK` 里的 `undefined` 在按位或里当 0，**锁标志消失、`openSync` 照常成功、全部测试照绿**。这是这个项目栽的第六次「恒绿」，也是唯一一次**在写代码之前**被抓到的（因为先跑了一句 `node -e` 去看那个常量在不在）。替代方案的三处防御全部是评审推演出来的、各配一条接缝测试：拿到锁后回读确认是自己的、抢陈旧锁后核对挪到手的是哪一个、释放前确认锁还是自己的——缺任何一条都会让两个进程同时进临界区。**第二件大事：MCP 工具从三个 PreToolUse hook 旁边过去**（它们只挂在 Bash 工具上），也就是说不做任何事的话，Phase 3 的净效果是「把呈折从一条有三道闸门的路搬到一条没有闸门的新路上」；已在工具里补了两条，而 BACKLOG ⑤ 记的「真闭合只有仓侧 CI 一条路」仍挂着等 Charlie 拍板。**第三件：`audit_folders --fix` 实现到一半发现两件事都不能做**——补回奏对标记只补得成当前会话的 id（编一个，§4.4 禁止）、draft 转正 REST 不支持只能 GraphQL（而开放 `POST /graphql` 等于开放全部 mutation），于是它变成纯只读工具，连带 `PATCH /pulls/{n}` 从写白名单删掉，**整个 server 只能发两种写请求**。三轮评审里**跨系统那轮又是最值钱的**（与 Phase 2 同）：读 门下 源码查出标记格式两边对不上（这边校验比 门下 松，传个 UUID 进来会「验证通过」而按钮静默不出现）、图被拍平导致既有布局的引用变断图、回话前缀会把块级 markdown 读成行内 code span。**变异战役 24+13 条全杀，但我的变异脚本自己撒了两次谎**：一次 perl 漏写 `/g` 只替了第一处，一次 `git diff --quiet` 跟 HEAD 比而工作树满是未提交改动——两次都把 NOOP 报成 SURVIVE。第一次让我差点把一个不存在的洞记下来，第二次让三条真变异被误判。**存活的结论必须先核实变异真的变到了那儿。** |
| 2026-07-30 | 2 | **三轮评审各挖到不同层的东西，第三轮（跨系统）最值钱。** 第一轮抓到四条我引入的回归，395 个测试一个都没抓到：中文名文件全挂（git 默认转义非 ASCII 路径，#31 那 22 篇全踩上）、大文档被 ENOBUFS 吞成「不存在」、行首反引号开出假围栏把整篇当代码剥掉、带空格文件名断图漏报。第二轮专职核实修复，判出**六条是我修出来的新问题**，最重的是 `npm test` 恒返回 0（为了「红了也打印读数」把 `&&` 换成 `;`）；同时查清老脚本 `folder-lint.sh:58` 的 `$ZH（` 在 UTF-8 locale 下让**整个脚本打印「体例合格」并 exit 0** —— 「缺中文版」这一整类从上线起就没拦住过，而闸门以老的为准。第三轮读 门下 源码，找到**三条规则与产品实际行为错位**：① 互链头规则的立身理由是假的（`lang.js` 只按文件名配对、从不读首行），而它是硬伤，实测正拦着渲染完全正常的 #12；② 图片引用该按文档自身目录解析（`render.js:26` 就是这么写的），一律按 `docs/` 拼会让子目录文档的断图**漏报**——正是规则 4 唯一要防的那件事；③ 修好假通过之后规则 1 第一次生效，把 22 章中文读物拦死，需要 `.monolingual` 登记豁免。疤痕清单杀伤率 12/34 (35%) → 全量 53/59 (90%)。**恒绿测试第五次**（parity 的守卫测试把未知行放末尾，无论有没有守卫都是 false）。 |
| 2026-07-29 | 1 | **双 review 在写代码前推翻了 design 的核心。** 两个独立 agent 各自查出三条高危，根因同一个：design 的数据模型里没有「我方回话」这个概念，而 requirements 有。派生出「#18 未回数应为 0」这条验收判据在真数据上不可满足、**最后一条判（几乎永远是我方回话）会被判成他的未回批注**（有判的 3 折里 2 折会常态谎报）、以及 `line` 字段实测 100% 为 null。随后实测找到 design v1 认为不存在的判别信号：**门下 提交批注的 review body 是固定串「御笔朱批 · N 条」，而 门下 根本没有发 reply 的代码路径**，于是 inline 侧的作者可以确定判定、且追溯有效。design 与 tasks 均重写为 v2。实现过程中守卫的**自验用例抓到守卫自己的漏洞**（只匹配 `-X POST` 连写、漏了参数被拆成数组元素的写法，也漏了 `gh api -f` 隐式 POST）。此后又跑了三轮对抗性代码评审（共 6 个独立 agent），报出 31+2 个问题。最值钱的三条都不是算法写错，是**测试覆盖的形状与生产调用的形状对不上**：`octokit.request('GET /x',{method:'POST'})` 真的会发 POST（Octokit 的 options 覆盖 method），所以「route 必须 GET 开头」是假闸门，改成运行时 hook；文本守卫从没对本项目生效过（真实写法是 `oc.request`，变量不叫 octokit）；以及 **门下 有一条降级路径会把锚不到行的涂归塞进 review body**，只要有一条降级，整批成功锚上的批注都会被判成「我方的、已回」——而「隔夜草稿 + agent 推过改版」正是最常见的形态。最终 119 条单测 + 41 条实机断言全绿。 |
| 2026-07-28 | 0 | 上线路上**连踩三次误伤，同一个病根：把「字符串里出现」当成「这条命令要执行」**。① 扫整条命令行，`create -R 别的仓 && view -R 奏折仓` 被拦——两个关键词分属不同段。改成按 `&& \|\| ; \|` 切段。② 拦了守卫作者自己的 `git commit`——commit message 里写了那个命令名，cwd 又恰在敕草仓 worktree。改成剥掉 env 前缀、`bash -c` 包壳、绝对路径后，**段首必须是 `gh`**。③ 剥壳的 `sed` 用了基本正则的交替，**BSD sed 根本不支持**——那条规则完全失效而且静默，`bash -c` 包壳绕过一路畅通。只有测试抓到了，而 `folder-lint.sh` 顶部本来就写着这一类坑的警告。 |

## 怎么让这份文件不烂掉

门下 自己的台账停摆过 7 个 commit，期间有指标一直处于触发状态。那次复盘的结论值得原样抄过来：**教训不是「忘了」，是记账没有触发器**——功能 commit 和量测 commit 各走各的，中间只靠自觉连着。

**触发器已于 2026-07-29 随 Phase 1 装上**（`npm test` 结束跑 `scripts/print-milestones.mjs`，从本文件解析状态行并打印，不硬编码——硬编码等于又造一个会漂的副本）。这笔欠账还清了。

仍然成立的一条：**看到「未开始」原样没动，要当成「没核实过」而不是「确实准确」**。触发器只保证你每次跑测试都会看见这张表，不保证表是对的。
