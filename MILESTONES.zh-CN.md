[English](MILESTONES.md) · **中文**

# 里程碑

落地顺序在 [SPEC §8](SPEC.zh-CN.md)。这份补的是 spec 没有的那一半：**凭什么算做完。**

**「写完」不等于「做完」。** Phase 0 写完花了约一小时，做完是三小时后——中间隔着三次误伤，全靠测试才暴露，其中一次是**静默的安全洞**。所以下面每条判据都是可证伪的、且必须真观察到，不接受「代码写了」。

## 各阶段

| 阶段 | 完工判据（可证伪） | 状态 |
|---|---|---|
| **0 · 路由守卫** | `guard-pr-create.test.sh` 21/21；两条路径实机验过（该拦的真被拒、该放的真通过）；SKILL.md 与 SPEC §7 与代码三方一致 | **✅ 2026-07-28** |
| **1 · 只读工具** ✅ | `read_comments` 在真折上返回真批注，`answered` 与手数对得上；`list_folders` 的未回数一致 | **✅ 2026-07-29** |
| **2 · lint_folder** | 对全部 open 折 + 每条规则至少一个必失败样本跑 differential，新旧输出逐条对齐，差异要么修掉要么写进 SPEC §5.2 说清 | 未开始 |
| **3 · 写入工具** | 用 `open_folder` 端到端真呈一折；两个并发 session **实测**不互踩（要看到 flock 生效，不是假设）；标记从线上 PR body 回读确认 | 未开始 |
| **4 · 退休** | 四个 bash 脚本删掉后走完整整一轮：呈折 → 读批注 → 回话；SKILL.md 瘦成名词表 + 工具指路；hook 拒绝信息改指 MCP 工具；SPEC §5.3 #6 那笔文档账还清 | 未开始 |

**最大的风险现在在 Phase 2，不在 Phase 1。** 原本 Phase 1 扛的是「agy 能不能挂上 MCP」——那条 2026-07-29 随 agy 出局而作废（[SPEC §1](SPEC.zh-CN.md)），不是被验证掉的，是被决策绕过的。**风险没有消失，只是换了位置**：现在最可能毁掉这个项目的是 Phase 2 的 lint 移植悄悄丢掉既有修复。那不是个能靠观察发现的失败——脚本删了、测试绿了、错误也照样发生，只有 differential test 拦得住。

## 实际发生了什么

按阶段追加，永不改写旧条目。

| 日期 | 阶段 | 发生了什么 |
|---|---|---|
| 2026-07-29 | 1 | **双 review 在写代码前推翻了 design 的核心。** 两个独立 agent 各自查出三条高危，根因同一个：design 的数据模型里没有「我方回话」这个概念，而 requirements 有。派生出「#18 未回数应为 0」这条验收判据在真数据上不可满足、**最后一条总批（几乎永远是我方回话）会被判成他的未回批注**（有总批的 3 折里 2 折会常态谎报）、以及 `line` 字段实测 100% 为 null。随后实测找到 design v1 认为不存在的判别信号：**zhupi 提交批注的 review body 是固定串「御笔朱批 · N 条」，而 zhupi 根本没有发 reply 的代码路径**，于是 inline 侧的作者可以确定判定、且追溯有效。design 与 tasks 均重写为 v2。实现过程中守卫的**自验用例抓到守卫自己的漏洞**（只匹配 `-X POST` 连写、漏了参数被拆成数组元素的写法，也漏了 `gh api -f` 隐式 POST）。最终 57 条单测 + 34 条实机断言全绿。 |
| 2026-07-28 | 0 | 上线路上**连踩三次误伤，同一个病根：把「字符串里出现」当成「这条命令要执行」**。① 扫整条命令行，`create -R 别的仓 && view -R 奏折仓` 被拦——两个关键词分属不同段。改成按 `&& \|\| ; \|` 切段。② 拦了守卫作者自己的 `git commit`——commit message 里写了那个命令名，cwd 又恰在奏折仓 worktree。改成剥掉 env 前缀、`bash -c` 包壳、绝对路径后，**段首必须是 `gh`**。③ 剥壳的 `sed` 用了基本正则的交替，**BSD sed 根本不支持**——那条规则完全失效而且静默，`bash -c` 包壳绕过一路畅通。只有测试抓到了，而 `folder-lint.sh` 顶部本来就写着这一类坑的警告。 |

## 怎么让这份文件不烂掉

zhupi 自己的台账停摆过 7 个 commit，期间有指标一直处于触发状态。那次复盘的结论值得原样抄过来：**教训不是「忘了」，是记账没有触发器**——功能 commit 和量测 commit 各走各的，中间只靠自觉连着。

**这份文件现在同样没有触发器**，这是已知的欠账，不是疏忽。之所以暂时可接受，只因为只完成了一个阶段、表还很短。等 Phase 1 落代码时触发器随之装上：测试脚本打印本表的状态行，每跑一次都被迫看见。在那之前，**看到「未开始」原样没动，要当成「没核实过」而不是「确实准确」**。
