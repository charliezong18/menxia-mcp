[English](BACKLOG.md) · **中文**

# BACKLOG

想清楚了但没排期的东西。

**排期规矩**：在 [MILESTONES](MILESTONES.zh-CN.md) 的 Phase 4 落地之前——也就是 bash 脚本退休、并且用 MCP 工具跑通整整一轮之前——这里一条都不碰。在那之前加进来的东西，抢的是一个还没闭合的循环的资源。窗口期内只修 bug 和真正挡路的摩擦。**没有真实呈折踩到的问题，不新增 B 条目。**

## B1 · 一折装多篇文档

**是什么**：`open_folder` 现在假设一折一对双语。让一折能带好几篇。

**为什么**：一轮评审有时天然成组——一份 spec 加它的迁移记录，或者一批四篇评审存档一起翻译。今天要么开四折（等于四次阅读会话），要么开一折但悄悄违反 slug 约定。

**怎么做**：`docs[]` 本来就收列表，管道是通的；缺的是 lint 规则——现在配对是推出**一个** slug，得改成按 slug 分组。`folder-lint.sh` 其实已经在按 slug 分组了（剥掉 `.zh-CN` 再去重），所以是移植不是新写。

**未定**：zhupi 的阅读界面对多文档 PR 处理得够不够好，值不值得做。**动手前先确认**——这是前端问题不是 server 问题。

## B2 · 双语缺口直接派给翻译

**是什么**：`audit_folders --fix` 现在只报双语缺口、补不了。让它把缺口交给一个翻译 agent 再写回来。

**为什么**：那批「十一篇缺双语对」是靠手工派三批给翻译 agent 清掉的。这形状的活本来就该由工具持有。

**怎么做**：shell 调当时在用的翻译 agent（2026-07-29 起不再假定是 agy），沿用已经验证有效的 prompt 纪律（自包含、给全绝对路径、禁止自行搜目录、不确定就标注绝不编造），写回前先跑结构验收——行数、小节数、代码块数。

**未定**：机器该不该在没有人对照的情况下直接写入译文。唯一一次真跑的结果是**零高危但六处中危**，全是术语表机械替换造成的，靠人抓出来的。自动写入就等于把它们发出去了。

## B3 · 图片自动推断，不用显式传

**是什么**：`open_folder` 现在要显式给 `assets[]`。改成解析文档里的图片引用，照着拷。

**为什么**：多一件要记得的事就多一件会忘的事，而忘了它产生的正是 lint 存在的理由——断图。

**怎么做**：复用 `lint.ts` 里的引用扫描器——反正那条代码路径本来就得剥掉代码块与 inline code（SPEC §5.1）。

**未定**：要不要保留 `assets[]` 当覆盖用，兜住扫描器看不见的引用方式（HTML `<img>`，或者该带上但正文没链的文件）。

## B4 · open_folder 的预演模式

**是什么**：`open_folder({dryRun: true})`——所有检查照跑、body 照拼、标记照探，报告将要创建什么，然后在开 PR 前停住。

**为什么**：写入路径是出错代价最高的一条，而今天要演练它的唯一办法是真创建一个。

**怎么做**：锁和 worktree 照拿，这样分支基点检查才是真的；GitHub 调用之后的全部跳过。

**未定**：这值不值得单开一个参数，还是说一套像样的测试脚手架本来就白送这个能力。

## B5 · 支持多个奏折仓

**是什么**：现在仓名是单个环境变量。改成一组，按调用选。

**为什么**：投机性的。**记在这里是为了以后不用再争一遍**——在出现第二个仓之前，答案是不做。

**怎么做**：不适用。

**未定**：没有。这是一条带着理由的「不做」，不是计划。

## B6 · 有新批注就推一条

**是什么**：折上来了新批注就通知，而不是让 agent 反复轮询 `list_folders`。

**为什么**：现在的循环是「问一句他批了吗」，每次都要一个来回，而且多数时候答案是没有。

**怎么做**：**不属于 MCP server 的活**——server 只在被叫到时说话。这该由 GitHub Action 或现有的 ntfy 那套接手。记在这里是因为它是 `list_folders` 之后最自然的下一个念头，应该被指去别处而不是被反复重议。

**未定**：zhupi 自己 BACKLOG 的 F10（文档到了推一条）是不是已经覆盖了这件事的镜像，如果是，两条都该住那边。

## merged 列表的分页（2026-07-30 记，约 2026-08-中 会触发）

`list_folders({state:'merged'})` 取 `pulls?state=closed&per_page=100`，`get()` 见到
`rel="next"` 直接失败。第三轮评审算过：仓从 #1（07-26）长到 #31（07-30），约 7 折/天，
closed 现在 15 个，**两周后过 100**，届时这个工具整个挂掉。

「宁可明着失败也不静默少数」这条决定不变，但到时错误文本至少要给一条能用的绕路
（只列 open / 直接 `read_comments(pr)`）。现在不做是因为 open 那一侧远得多（16 个）。

## Phase 2 第一轮评审留下的账（2026-07-30 记）

**① 老脚本 `folder-lint.sh:58` 的 `$ZH（` 是 bash 未绑定变量。** 全角括号被算进标识符，
`set -u` 下让跑 slug 循环的子 shell **当场死掉** —— 所以它对「缺中文版」这一类
**从来没生效过**，而且排序在后的 slug 也全部不检查。同病还有 `open-folder.sh:65` 的 `$sec」`。

后果：对账窗口内「以老脚本为准」在这一条上是错的，这类折照样呈得上去。
不在 Phase 2 内是因为改老脚本等于动闸门本身；但**这条会让 differential 永久红一处**
（已在登记表里标成「老脚本 bug，新实现正确」）。

**② 老脚本会把 git 转义过的中文文件名直接打给人看**（`✗ guanzhi-00-\345...md" 缺英文版`），
因为它也没设 `core.quotePath=false`。显示 bug，判定是对的。

**③ 子目录文档的图片引用解析与 GitHub 不一致。** `docs/*.md` 的 pathspec `*` 跨 `/`，
所以 `docs/sub/x.md` 会被检查；但引用一律按 `docs/assets/` 解析，而 GitHub 按相对路径
（`docs/sub/assets/`）。双向都会给错结论。从老脚本继承，改它属于扩大范围。

**④ 缩进代码块和 HTML 注释里的图片引用会误报硬伤。** `stripCode` 只剥顶格围栏和行内 code；
嵌套列表里缩进 4 空格的围栏、`<!-- ![x](assets/y.png) -->` 都会被当成真引用。
「一篇讲 lint 规则的文档过不了 lint」这条病历只关掉了一半。

**⑤ 呈折闸门是「防手滑，不防绕过」。—— 2026-07-31 量过，暂不做，理由见末尾。**
第一轮评审实测 6 条旁路：
包一层脚本、`xargs`、`eval`、python `subprocess`、`git push` + 网页建 PR、
非 Claude Code 运行时（agy / 自己的终端）。三个 PreToolUse hook 只挂在 Claude Code 的
Bash 工具上，靠加匹配模式补不上这一层。

真要闭合只有一条路：**把判据下移到仓侧**（main 上加 CI 对 PR 校验体例）。
那是 Phase 3/4 的范围，而且要先跟 Charlie 确认 —— 加 CI 会改变他的呈折体感。

**2026-07-31 聊过了，结论：先不做。** 三个数各自独立指向同一边：

| 量的什么 | 结果 |
|---|---|
| 真的有人绕过吗 | **0/18** —— 全部 open 折都带「回奏对」标记，即全走的正路。那 6 条旁路是推演，实测发生率零 |
| 体例真漂了多少 | 17/18 干净，唯一那条是**误报**（#31 该走单语豁免） |
| 朱批台看得见 check 状态吗 | **看不见**。zhupi 源码 23 个文件里 `check_run` / `checkSuite` / `statuses` / `mergeable_state` 零命中 |

第三条是决定性的：CI 设成 required 的净效果是**在他看不见的地方亮红，然后挡住
他唯一在意的那个按钮（钦此）** —— 信号不可见 + 动作阻断。设成 advisory 就只是个
没人看的红叉。

**而那次量出来的唯一一条红，暴露的是另一笔真账**：`docs/.monolingual` 豁免机制
从加上起一次都没能用上（仓内文件，`open_folder` 没有路径能创建它），于是 #31 长期
报 22 条假硬伤。**一个开始报假红的检查，人就学会忽略它** —— 那比「有人绕过」
更现实地在毁掉这套闸门。已修（`open_folder` 的 `monolingual` 开关 + #31 补登记），
修完巡检 19/19 全干净。

**重开这条的判据**：出现第一例真绕过（open 折里出现不带「回奏对」标记的），
或者 `audit_folders` 连续报出非误报的漂移。在那之前，检测交给
`audit_folders()`（同一个核，扫全部 open 折，碰不到 merge 路径）。
