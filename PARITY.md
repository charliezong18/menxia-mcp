# 新旧 lint 对账台账

**退休条件（design D1，Charlie 2026-07-30 拍板）：连续 10 次呈折零分歧才允许删 `folder-lint.sh`。**

不定条件等于永久双份维护 —— 而「暂时并行」是技术债最常见的措辞。

对账窗口内**以老脚本为准**：并行的全部理由是观察新实现的回归，而观察收益两个方向都拿得到，
把新的设为权威只是单方面加风险。

这张表记的是**退出码**。退出码一致 ≠ 结论一致（都报错但报的是不同的错），
集合级对账跑 `npm run differential` —— 归一化逻辑只许有一份，写第二遍就是又造一个会漂的副本。

`SKIP_LINT 跳过` 那些行是 D8 定的：留着逃生口（「拦太死把人逼向绕过」那条教训），
但**让它可见**，否则一个无条件无日志的关闸开关等于这道闸门随时可以不存在。

**别手工编辑这张表。** 结果文本必须逐字等于 `退出码一致` / `**不一致**` /
`SKIP_LINT 跳过` / 三种「不计入」之一 —— 出现别的文本时 `print-parity` 会判定台账不可信、
**禁止据此退休**（第一轮评审：上一版靠子串「一致」判，`SKIP_LINT 跳过（其余与上次一致）`
会被算成一致，于是在闸门被关过的窗口里做不可逆决定）。

> **⚠️ 这张表从 2026-07-30（Phase 3）起停止增长。**
>
> 新的 `open_folder` 工具不跑老 bash lint，也就没有可对账的东西 —— 走 MCP 呈的折
> 一行都不会记进来。**「连续 10 次呈折零分歧才允许删 `folder-lint.sh`」这个判据因此
> 不可满足**：呈折都走新路了，老路不再被执行。
>
> 这不是绕过，是记下来等拍板。D10（上一折里提的「连续 10 次撑不住 —— 88% 的折两边
> 都零硬伤，凑满 10 次证明的是『两个都没炸』」）本来就还没定，现在多了一条：
> 判据换成什么，才能既支撑「删掉老脚本」这个不可逆决定，又在呈折已经搬走之后仍然可测。
> 挂在 #38 的「待你拍板」里。
>
> **在此之前，`folder-lint.sh` 不删。**

**已知缺一条（2026-07-30 · Phase 3 T0）。** `open-folder.sh` 的 `parity_row` 用
`$(printf ... '\n')` 拼行，而命令替换会剥掉尾换行；`parity_flush` 又用 `printf '%s'`
不补。于是第二行起全部粘在上一行末尾，`parseRows` 的 `/^\|\s*\d{4}-/` 只认行首那条
—— **#34 #35 两次真呈折在这张表上只算一次**（下面那行末尾的 `||` 就是它）。

写入侧已修：换行改在 flush 时补，文件末尾没换行先补一个。`open-folder.sh` 不在任何
仓里，改动只落在 `~/.claude/skills/review-loop/`。

**已粘的那行不手工拆开。** 少算的方向是保守的，而手工痕迹会让 `unknown` 判定失效
—— 那才是真危险（这张表自己在上面写着「别手工编辑」）。代价是 10 次里损失 1 次，认了。

| 时间 (UTC) | 折 | 旧 | 新 | 结果 |
|---|---|---|---|---|
| 2026-07-30T16:53Z | #34 | 0 | 0 | 退出码一致 || 2026-07-30T17:27Z | #35 | 0 | 0 | 退出码一致 |
| 2026-07-30T20:31Z | #39 | 0 | 0 | 退出码一致 |
