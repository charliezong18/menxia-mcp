[English](OPEN-QUESTIONS.md) · **中文**

# 悬而未决

**规矩：这里没有一条会阻塞。** 每条都带一个默认答案，工作照默认往下走。你有空扫一遍，只挑想改的说——不用逐条口试。答完的往下挪进《已定》。

## 未决（按默认答案照走）

**agy 能挂上 MCP server 吗？**
默认：假设能。这是唯一一条能让整个项目作废的问题——跨 harness 是建它的头号理由（[SPEC §1](SPEC.zh-CN.md)）。把它设成 Phase 1 的完工判据就是为了让它**早失败**，而不是等写入工具都做完了才发现。真答不能，Phase 3-4 得靠剩下两条收益（结构化读取、类型化入参）重新论证——那两条是真的，但小得多。

**公开仓要不要脱敏？**
默认：不脱。spec 里点了四次私有奏折仓的名字，MCP 配置示例里有一处家目录绝对路径。两样都不算秘密——账号本来就是公开的，而且它们在正文里都有实际作用。想换成占位符说一声。

**体例规则要不要做成可配置？**
默认：写死，只把仓名和 checkout 路径留成环境变量。把双语对、互链头、五段 body 都做成可配，是给一个人的用户群做投机性通用化。真有人 fork 了来问再说。

**flock 等多久放弃？**
默认：30 秒，然后返回一句能读懂的「另一个 session 正在呈折」。够熬过一次正常呈折，又短到不会让卡住的锁看起来像挂死。

**Phase 4 是真删脚本还是留着？**
默认：删。git 历史里有，而且**同一套规则存在两份活的实现**，正是 [SPEC §5.1](SPEC.zh-CN.md) 那批文档漂移的成因本身。

**英文版 spec 该住本仓还是只住奏折仓？**
默认：两边都放，就是现在这对 `SPEC.md` / `SPEC.zh-CN.md`。代价是每轮批注之后要手工同步，这个成本是真的，但不大。

## 已定

| 问题 | 结论 | 日期 |
|---|---|---|
| 到底要不要做 MCP？ | 做——但不是为了你说的那个理由。「更固定」是 hook 的活；MCP 买的是跨 harness、结构化读取、类型化入参 | 07-28 |
| MCP 能防绕过吗？ | 不能。路由是另一个问题、另一套药，作为 Phase 0 先上了 | 07-28 |
| 工具范围 | 六个。钦此（merge）刻意排除——那是人点的 | 07-28 |
| 包一层脚本还是重写？ | 全重写成 TypeScript 并补测试；脚本在 Phase 4 退休 | 07-28 |
| 住哪？ | 独立公开仓，与 zhupi 配成「产品 + agent 侧」一对 | 07-28 |
| 语言方向检测 | 按比例查：剥掉代码块与 inline code 后算中日韩字符占比——英文版 >30% 报错，中文版 <30% 报错 | 07-28 |
| 五段缺项，拦还是警告？ | 警告。代码是对的、文档是错的；文档账在 Phase 4 还 | 07-28 |
| 文档传路径还是传全文？ | 传路径。server 自己拷进去，agent 全程不碰奏折仓 | 07-28 |
| GitHub 认证 | 启动时借 `gh auth token`；不另管一份会过期的 PAT | 07-28 |
| 并发机制 | 文件锁 + 用完即弃的 worktree。stdio server 是每会话一进程，自己做不到串行 | 07-28 |
| 回奏对标记探不到时怎么办？ | 什么都不埋。静默指错比没有按钮更糟 | 07-28 |
| 装法 | MCP 配置里写绝对路径。不用 `npm link`——那个全局单指针已经搞挂过一次线上 | 07-28 |
